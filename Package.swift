// swift-tools-version:5.0

import PackageDescription

let package = Package(
    name: "X",
    products: [
        .library(name: "X", targets: ["X"])
    ],
    targets: [
        .target(name: "X")
    ]
)
